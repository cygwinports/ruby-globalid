inherit rubygem

NAME="ruby-globalid"
VERSION=0.3.6
RELEASE=1
CATEGORY="Ruby"
SUMMARY="Rails models URI reference support"
DESCRIPTION="GlobalID allows you to reference models with a URI."

ARCH=noarch

PKG_NAMES="${NAME} ${NAME}-doc"
ruby_globalid_CONTENTS="${RUBYGEM_INSTDIR#/} ${RUBYGEM_SPEC#/}"
ruby_globalid_doc_CONTENTS="${RUBYGEM_DOCDIR#/}"

